import React from "react";
import { render } from "@testing-library/react";

import { ANS } from "./ANS";

describe("Wallet", () => {
  test("renders the Wallet component", () => {
    render(<ANS />);
  });
});